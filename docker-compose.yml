services:
  free-bot:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    networks:
      - default
      - claude-proxy_default

networks:
  claude-proxy_default:
    external: true
